services:
  garmin-workouts-mcp-fork:
    image: garmin-workouts-mcp:fork-strength
    build:
      context: ../../
      dockerfile: deploy/synology/Dockerfile
    container_name: garmin-workouts-mcp-fork
    restart: unless-stopped
    env_file:
      - .env
    environment:
      GARTH_HOME: /garth
      MCP_HOST: "0.0.0.0"
      MCP_PORT: "8001"
      MCP_PATH: "/"
      GARMIN_STRENGTH_PAYLOAD_LOG_DIR: /app/logs/strength_payloads
    volumes:
      - ./garth:/garth
      - ./logs:/app/logs
    ports:
      - "8001:8001"
